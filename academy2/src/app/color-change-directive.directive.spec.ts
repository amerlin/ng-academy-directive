import { ColorChangeDirectiveDirective } from './color-change-directive.directive';

describe('ColorChangeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorChangeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
